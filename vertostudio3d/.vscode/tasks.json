{
    "tasks": [
        {
            "type": "shell",
            "label": "make",
            "command": "make clean-windows; make",
            "args": [
                //remember these are the flags/args passed to gcc so your flags are needed here
                // "-g",
                // "${file}",
                // "-o",
                // "${fileDirname}\\${fileBasenameNoExtension}.exe",
                // "-lmingw32",
                // "-lsdl2main",
                // "-lsdl2",
                // "-mwindows",
                // "-lSDL2_image",
                // "-lSDL2_ttf"
            ],
            "options": {
                "cwd": "${fileDirname}"
            }
        }
    ],
    "version": "2.0.0"
}